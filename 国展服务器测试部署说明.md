# 国展 Piwigo 服务器测试部署说明

更新时间：2026-07-12

## 包内内容

本目录是完整 Piwigo 16.4.0 测试项目，已内置：

- 前端主题：`themes/guozhan-gallery`
- 客户后台插件：`plugins/GuozhanClientAdmin`
- Piwigo 原始核心文件、默认主题、默认插件

## 全新服务器测试

1. 将压缩包内的 `piwigo` 目录内容上传到服务器站点根目录。
2. 给 Piwigo 需要写入的目录配置权限，重点检查 `_data/`、`upload/`、`galleries/`、`local/`。
3. 访问站点域名，按 Piwigo 安装向导填写数据库信息并完成安装。
4. 登录后台，启用插件 `GuozhanClientAdmin`。
5. 在后台启用主题 `guozhan-gallery`，并设为默认主题。
6. 进入“国展客户后台”，执行或检查分类合同同步。
7. 检查健康状态：数据库表、分类合同、上传合同、WebService、前台同步、主题状态都应为正常。
8. 分别向普通分类、直达分类、预留画展和比赛类型上传测试作品。
9. 前台检查：首页、分类、比赛、搜索、详情、联系客服是否同步后台内容。

## 合并到已有 Piwigo

1. 先备份服务器文件和数据库。
2. 将 `themes/guozhan-gallery` 上传到服务器 Piwigo 的 `themes/`。
3. 将 `plugins/GuozhanClientAdmin` 上传到服务器 Piwigo 的 `plugins/`。
4. 启用插件和主题，再执行分类合同同步。
5. 不要覆盖服务器已有的 `local/config`、`_data`、`upload`、`galleries`，除非已经确认是全新测试站。

## 已完成对接

- 10 个顶层板块、29 个子板块。
- 39 个前端可编辑上传位置，包括全部顶层板块、子板块和 7 个预留未知画展。
- 比赛页接入 `gzca.competition.getMedia` 和 `gzca.images.getList` 的 `competition` 筛选。
- 客服和品牌接入 `gzca.contact.get`。
- 列表优先读取 `thumbnail_url`，详情优先读取 `display_url`。
- 后台上传、分类、画展、作品、比赛、客服接口均已覆盖。

## 本地验证记录

- `node tools/verify-backend.js`：通过。
- `node tools/verify-contract.js`：通过。
- `node tools/test-mock-api.js`：通过。
- 主题 JS `node --check`：通过。

本机没有可用的 `php` 命令，因此 PHP 语法检查请在服务器或本地 PHP 环境中补跑。
